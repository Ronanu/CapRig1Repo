version: "3.8"
services:
  microros-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "${AGENT_PORT:-8888}:${AGENT_PORT:-8888}/udp"
    restart: unless-stopped
